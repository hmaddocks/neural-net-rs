[workspace]

members = [
    "autograd",
    "matrix",
    "neural-network",
    "mnist",
    "mnist-demo",
    "gpt",
]
resolver = "2"

[workspace.lints.clippy]
all = "warn"

[profile.bench]
debug = true
